santalucia:      
  ams:
    datasources:
      informix:
        hikari:          connection-timeout: 100
          pool-name: InformixCP
          jdbc-url: jdbc:h2:mem:db-informix;DB_CLOSE_DELAY=-1
          driver-class-name: org.h2.Driver
      oracle:
        hikari:
          username: sa
          password: sa
          connection-timeout: 100
          pool-name: OracleCP
          jdbc-url: jdbc:h2:mem:db-oracle;DB_CLOSE_DELAY=-1;MODE=Oracle
          driver-class-name: org.h2.Driver